# CuotaData26 App
Pequeña app Flask para gestionar modo carrera y analizar partidos reales.


## Deploy en Render
- Build command: `pip install -r requirements.txt`
- Start command: `gunicorn app:app`
